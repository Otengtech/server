services:
  - type: web
    name: audio-extractor
    env: node
    buildCommand: |
      pnpm install
      chmod +x node_modules/@ffmpeg-installer/linux-x64/ffmpeg
      chmod +x node_modules/@ffprobe-installer/linux-x64/ffprobe
    startCommand: npm start
    healthCheckPath: /health